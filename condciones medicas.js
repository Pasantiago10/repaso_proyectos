let Patient = "cured";
let history = "this patient surffer of cancer";

while (Patient, history) {
    if (Patient === history)  {
        console.log("this patient send to home"); 
    } else if(Patient != history ) {
        console.log("this patient need more medic thrstamient");
        
    } 
    break;
}